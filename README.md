# EvolveAI Companion: Платформа для создания ИИ-компаньонов с динамической личностью

**EvolveAI Companion** — это не просто чат-бот. Это продвинутая платформа для создания виртуальных собеседников, построенная на базе Google Gemini, чья личность, стиль общения и глубина отношений с пользователем динамически изменяются со временем. В основе проекта лежит уникальная система эволюции отношений и когнитивный цикл анализа диалогов, которые создают по-настоящему живого и "помнящего" компаньона.

## 🌟 Ключевые концепции и философия

Проект строится на трех фундаментальных столпах, которые отличают его от стандартных чат-ботов:

### 1. **Динамическая личность (Движок отношений)**
Вместо статичного системного промпта, личность ИИ-компаньона определяется его **уровнем отношений** с пользователем. Система насчитывает 14 уровней (от "Незнакомца" до "Романтического партнера"), каждый из которых имеет собственный набор:
*   **Контекста и правил поведения**: Как ИИ должен себя вести.
*   **Запрещенных тем**: О чем нельзя говорить на данном этапе.
*   **Примеров диалогов**: Для точной настройки стиля общения.

Промпт для модели генерируется "на лету" перед каждым запросом, что позволяет ИИ адаптировать свой тон, лексику и глубину ответов в соответствии с текущим уровнем отношений.

### 2. **Автономная память и анализ (Когнитивный цикл)**
Проект имитирует человеческую способность осмысливать прошлые разговоры. Это реализовано через фоновый асинхронный процесс:
*   **Суммаризация**: После накопления определенного количества сообщений, система запускает специальную, более мощную модель (`gemma-3-27b-it`), которая анализирует диалог.
*   **Психологический анализ**: Модель не просто сокращает текст, а выступает в роли "психолога-аналитика", оценивая **качество общения** по шкале (от -5 до +10) и формируя **ключевой вывод** о динамике отношений.
*   **Обновление профиля**: Полученная оценка (`relationship_score`) обновляется в профиле пользователя, напрямую влияя на его прогресс к следующему уровню отношений.
*   **Долгосрочная память**: Используя **Gemini Tools**, бот может в реальном времени принимать решение о сохранении важных фактов о пользователе (`save_long_term_memory`) или поиске информации в своей памяти (`get_long_term_memories`), что делает общение еще более персонализированным.

### 3. **Реалистичное взаимодействие**
Чтобы усилить эффект "живого" общения, используются различные UX-техники:
*   **Имитация набора текста**: Задержки перед отправкой сообщения рассчитываются на основе его длины.
*   **Разделение мыслей**: Ответы, содержащие `||`, отправляются по частям с небольшими паузами.
*   **Голосовые сообщения**: Бот может преобразовывать ответы в речь (TTS) и отправлять их в виде "спонтанных" голосовых сообщений.
*   **Понимание изображений**: Пользователь может отправлять фотографии, а бот, благодаря мультимодальности Gemini, сможет их "увидеть" и прокомментировать.

## ✨ Основные возможности

*   🧠 **Система эволюции отношений**: 14-уровневая прогрессия с уникальными правилами и стилем общения для каждого уровня.
*   💾 **Два типа памяти**:
    *   **Краткосрочная**: История последних сообщений.
    *   **Долгосрочная**: База данных фактов, управляемая через Gemini Tools.
*   🔄 **Когнитивный цикл анализа**: Автономная фоновая задача для анализа диалогов, оценки качества общения и обновления "очков отношений".
*   🗣️ **Голосовые сообщения**: Встроенная поддержка Text-to-Speech для создания более живого опыта.
*   🖼️ **Поддержка изображений**: Возможность отправлять и получать комментарии к фотографиям.
*   ❓ **Интерактивное знакомство**: FSM-анкета для сбора базовой информации о новом пользователе (имя, пол, город для определения часового пояса).
*   🚀 **Производительность и масштабируемость**:
    *   Асинхронная архитектура на FastAPI и Aiogram 3.
    *   Поддержка PostgreSQL для продакшн-окружения и SQLite для легкого старта.
    *   Оптимизированные запросы к БД (UPSERT).
    *   Ограничение частоты запросов (Rate Limiting) для защиты от спама.
*   ⚙️ **Готовность к развертыванию**:
    *   Полная поддержка контейнеризации с помощью Docker и Docker Compose.
    *   Мультистадийный Dockerfile для создания легковесного и безопасного образа.
    *   Эндпоинты для проверки состояния сервиса (`/health`, `/ready`).
    *   Интеграция с Prometheus для сбора метрик.
*   💵 **Монетизация**: Система уровней разделена на бесплатные (1-7) и платные (8-14), что закладывает основу для внедрения подписок.

## ⚡ Производительность и масштабируемость

**EvolveAI оптимизирован для высокой нагрузки:**
- 🚀 **Redis кэширование** с hit rate 60-80% - снижает нагрузку на БД
- 🔄 **Connection pooling** для PostgreSQL (20+10) и Redis (50) - поддержка 30-50 concurrent запросов
- 📊 **Circuit Breaker** для Redis - graceful degradation при сбоях
- 📈 **Мониторинг** - endpoints для метрик БД и кэша

**См. подробнее:** [SCALABILITY_IMPROVEMENTS.md](SCALABILITY_IMPROVEMENTS.md)

## 🛠️ Техническая архитектура

Проект разделен на два независимых, но взаимосвязанных сервиса, что соответствует современным практикам микросервисной архитектуры.

1.  **Бэкенд-сервер (FastAPI) - "Мозг"**
    *   **Роль**: Предоставляет REST API для всей бизнес-логики.
    *   **Задачи**:
        *   Взаимодействие с Google Gemini API для генерации ответов и анализа диалогов.
        *   Управление профилями пользователей, историей чатов и памятью в базе данных.
        *   Реализация логики эволюции отношений и когнитивного цикла.
        *   Генерация голосовых сообщений (TTS).
    *   **Стек**: FastAPI, Pydantic, SQLAlchemy (async), PostgreSQL/SQLite, Tenacity.

2.  **Telegram-бот (Aiogram 3) - "Интерфейс"**
    *   **Роль**: Является точкой входа для пользователя в Telegram.
    *   **Задачи**:
        *   Обработка команд (`/start`, `/reset`) и сообщений от пользователя.
        *   Управление состояниями (FSM) во время анкетирования (с использованием Redis).
        *   Взаимодействие с бэкенд-сервером через отказоустойчивый HTTP-клиент (`httpx` с retry-логикой).
        *   Реализация UX-улучшений (имитация набора текста).
    *   **Стек**: Aiogram 3, Redis, httpx.

### 🌊 Поток данных (одно сообщение)

`Пользователь` → `Telegram Bot (Aiogram)` → `HTTP POST /chat` → `Backend (FastAPI)` → `Gemini API` → `Backend` → `HTTP Response` → `Telegram Bot` → `Пользователь`

### 🔄 Поток данных (когнитивный цикл)

`Backend (Trigger)` → `Database (get messages)` → `Gemini API (analyze)` → `Backend (parse analysis)` → `Database (update score, save summary, delete old messages)` → `Backend (check for level up)`

## 🚀 Установка и запуск

Рекомендуемый и самый простой способ запуска — использование Docker Compose.

## 📢 Массовая рассылка сообщений

Для отправки сообщений всем пользователям, которые писали боту, создан специальный скрипт `broadcast.py`.

### Использование

1. Убедитесь, что вы находитесь в корневой директории проекта
2. Запустите скрипт с текстом сообщения:
   ```bash
   python broadcast.py "Привет"
   ```

Скрипт автоматически получит список всех пользователей из базы данных и отправит им указанное сообщение.

### Требования
*   Docker и Docker Compose
*   Git
*   API-ключ для **Google Gemini**
*   Токен для **Telegram-бота**, полученный у [@BotFather](https://t.me/BotFather)

### 1. Настройка окружения

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
    cd EvolveAI-Companion
    ```

2.  **Создайте файл с переменными окружения:**
    Скопируйте `env.example` в `.env` и заполните его своими данными.
    ```bash
    cp .env.example .env
    ```
    Содержимое `.env`:
    ```env
    # --- Обязательные переменные ---
    # Токен вашего Telegram-бота
    TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН_ОТ_BOTFATHER"
    # Ваш API-ключ от Google AI Studio
    GOOGLE_API_KEY="ВАШ_API_КЛЮЧ_GEMINI"

    # --- Настройки базы данных PostgreSQL (рекомендуется для продакшена) ---
    # Эти значения уже настроены для работы с docker-compose.yml
    POSTGRES_USER=myuser
    POSTGRES_PASSWORD=mypassword
    POSTGRES_DB=evolvedb
    DB_HOST=db

    # --- Настройки Redis (для FSM в боте) ---
    # Эти значения уже настроены для работы с docker-compose.yml
    REDIS_HOST=redis
    ```

### 2. Запуск с помощью Docker Compose

Этот метод автоматически поднимет все необходимые сервисы: бэкенд, Telegram-бот, базу данных PostgreSQL и Redis.

1.  **Соберите и запустите контейнеры в фоновом режиме:**
    ```bash
    docker-compose up --build -d
    ```

2.  **Просмотр логов (если нужно):**
    ```bash
    docker-compose logs -f            # Показать логи всех сервисов
    docker-compose logs -f backend    # Показать логи только бэкенда
    docker-compose logs -f bot        # Показать логи только бота
    ```

3.  **Остановка:**
    ```bash
    docker-compose down
    ```

Теперь ваш бот запущен и готов к общению в Telegram!

### 3. Ручной запуск (для разработки)

Если вы хотите запускать сервисы отдельно без Docker.

1.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Настройте базу данных в `config.py`:**
    Вы можете закомментировать блок для PostgreSQL и раскомментировать строку для SQLite для простого локального запуска без настройки БД.

3.  **Запустите Alembic миграции (если используется PostgreSQL):**
    *требуется установить `alembic` (`pip install alembic`)*
    ```bash
    alembic upgrade head
    ```

4.  **Запустите бэкенд-сервер:**
    В одном терминале:
    ```bash
    uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ```

5.  **Запустите Telegram-бота:**
    Во втором терминале (убедитесь, что Redis запущен, если используете FSM):
    ```bash
    python -m bot.bot
    ```

## 📂 Структура проекта

```
.
├── bot/                  # Код Telegram-бота (Aiogram)
│   ├── bot.py            # Точка входа, инициализация Dispatcher
│   └── handlers.py       # Обработчики команд, сообщений и FSM-логика
├── server/               # Код бэкенд-сервера (FastAPI)
│   ├── ai.py             # Основная логика взаимодействия с Gemini API
│   ├── database.py       # Функции для работы с базой данных (SQLAlchemy)
│   ├── models.py         # Модели таблиц SQLAlchemy
│   ├── schemas.py        # Схемы Pydantic для валидации API-запросов
│   ├── summarizer.py     # Логика когнитивного цикла: анализ и суммаризация
│   ├── relationship_logic.py # Логика проверки и повышения уровня отношений
│   └── tts.py            # Синтез речи
├── .env.example          # Пример файла с переменными окружения
├── config.py             # Глобальная конфигурация проекта
├── docker-compose.yml    # Оркестрация сервисов для Docker
├── Dockerfile            # Инструкции для сборки Docker-образа бэкенда
├── main.py               # Точка входа для FastAPI-сервера
├── personality_prompts.py # Конкретная личность ИИ
├── prompts.py            # Базовые системные промпты
├── relationship.md       # Концептуальное описание уровней отношений
├── relationship_config.py# Конфигурация каждого уровня отношений для промптов
└── requirements.txt      # Список зависимостей Python
```